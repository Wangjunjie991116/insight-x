# 代码注释（简体中文）

## 语言

- 业务代码中的注释与文档字符串使用**简体中文**。
- 例外：许可证头、必须与上游一致的文案、对外错误码文档若固定为英文可保留英文。

## 必须覆盖的位置

- **流程**：编排层、状态迁移、多阶段异步任务 —— 在阶段边界说明意图与顺序依赖。
- **核心函数**：模块对外入口、`execute`/`run`、复杂分支集合 —— 说明参数、返回值、异常或失败时的语义。

## 避免

- 逐字翻译代码的无信息量注释。
- 同一注释块内中英文混杂（专有名词除外）。
