---
description: 代码注释使用简体中文；流程与核心函数需有必要说明
alwaysApply: true
---

# 注释语言与粒度

- **语言**：业务代码中的注释（含模块/类/函数文档字符串、行间说明）使用**简体中文**。例外：第三方协议要求的英文、自动生成的许可证头、以及必须与上游保持一致的字符串可保持原文。
- **不要英中混写同一句话**；标识符与 API 名称保持英文。
- **粒度**：流程分叉、跨模块约定、非显而易见的算法与安全假设写清楚；显而易见的一行代码不必重复注释。
- **流程**：对状态机、流水线、Agent 编排、异步任务阶段等，在步骤边界用简短中文说明「做什么、为何这样做」。
- **核心函数**：对入口函数、对外 API、`execute`/`run` 类核心方法，用文档字符串或模块注释说明输入输出、失败语义与调用前提。
